################
# BCH Explorer #
################

# Block the internal APIs
location /chipnet/api/internal/ {
	return 404;
}
location /chipnet/api/v1/internal/ {
	return 404;
}

# websocket has special HTTP headers
location /chipnet/api/v1/ws {
	rewrite ^/chipnet/(.*) /$1 break;
	try_files /dev/null @bch-explorer-chipnet-api-v1-websocket;
}

# warm cache mining and mempool API responses
location /chipnet/api/v1/statistics {
	rewrite ^/chipnet/(.*) /$1 break;
	try_files /dev/null @bch-explorer-chipnet-api-v1-cache-warm;
}
location /chipnet/api/v1/mining {
	rewrite ^/chipnet/(.*) /$1 break;
	try_files /dev/null @bch-explorer-chipnet-api-v1-cache-warm;
}

# it's ok to cache blockchain data "forever", so we do 30d
location /chipnet/api/v1/block/ {
	rewrite ^/chipnet/(.*) /$1 break;
	try_files /dev/null @bch-explorer-chipnet-api-v1-cache-forever;
}

# everything else gets "normal" cache
location /chipnet/api/v1 {
	rewrite ^/chipnet/(.*) /$1 break;
	try_files /dev/null @bch-explorer-chipnet-api-v1-cache-normal;
}

################
# API backends #
################

# it's ok to cache blockchain data "forever", so we do 30d
location /chipnet/api/block/ {
	rewrite ^/chipnet/api/(.*) /$1 break;
	try_files /dev/null @bch-explorer-chipnet-api-cache-forever;
}
# other API responses cannot be cached
location /chipnet/api/ {
	rewrite ^/chipnet/api/(.*) /$1 break;
	try_files /dev/null @bch-explorer-chipnet-api-cache-disabled;
}

###########
# routing #
###########

location @bch-explorer-chipnet-api-v1-websocket {
	proxy_pass $bchExplorerChipnet;
	proxy_http_version 1.1;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header Upgrade $http_upgrade;
	proxy_set_header Connection "upgrade";
	proxy_set_header X-Forwarded-Proto $scheme;
}

location @bch-explorer-chipnet-api-v1-cache-forever {
	proxy_pass $bchExplorerChipnet;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;

	proxy_cache_background_update off;
	proxy_cache_use_stale error;
	proxy_cache apicold;
	proxy_cache_valid 200 30d;
	proxy_redirect off;

	expires 30d;
}

location @bch-explorer-chipnet-api-v1-cache-warm {
	proxy_pass $bchExplorerChipnet;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;

	proxy_cache_background_update off;
	proxy_cache_use_stale error;
	proxy_cache apiwarm;
	proxy_cache_valid 200 10s;
	proxy_redirect off;
}

location @bch-explorer-chipnet-api-v1-cache-normal {
	proxy_pass $bchExplorerChipnet;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;

	proxy_cache apinormal;
	proxy_cache_valid 200 10s;
	proxy_redirect off;

	expires 10s;
}

location @bch-explorer-chipnet-api-v1-cache-disabled {
	proxy_pass $bchExplorerChipnet;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;

	proxy_redirect off;
	proxy_buffering off;

	expires -1;
}

location @bch-explorer-chipnet-api-cache-disabled {
	proxy_pass $bchExplorerChipnet;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;

	proxy_redirect off;
	proxy_buffering off;

	expires -1;
}

location @bch-explorer-chipnet-api-cache-forever {
	proxy_pass $bchExplorerChipnet;

	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	proxy_set_header X-Forwarded-Proto $scheme;

	proxy_cache_background_update off;
	proxy_cache_use_stale error;
	proxy_cache apicold;
	proxy_cache_valid 200 30d;
	proxy_redirect off;

	expires 30d;
}
