# Bitcoin Cash Node (node only) for Umbrel
# Fulcrum is now its own app (gavin-fulcrum). ZMQ added for SoloStrike Cash.
services:
  app_proxy:
    environment:
      APP_HOST: gavin-bitcoin-cash-node_web_1
      APP_PORT: 3000

  tor:
    image: getumbrel/tor:0.4.7.8
    restart: on-failure
    environment:
      HOME: /tmp
    volumes:
      - ${APP_DATA_DIR}/torrc:/etc/tor/torrc:ro
      - ${APP_DATA_DIR}/data/tor:/data
    networks:
      default:
        ipv4_address: ${APP_GAVIN_BITCOIN_CASH_NODE_TOR_IP}

  bitcoind:
    image: zquestz/bitcoin-cash-node:29.0.0
    restart: always
    stop_grace_period: 1m
    volumes:
      - ${APP_DATA_DIR}/data/bitcoind:/data
    entrypoint: ["bitcoind"]
    command:
      - -datadir=/data
      - -printtoconsole
      - -server=1
      - -listen=1
      - -port=8335
      - -rpcbind=0.0.0.0
      - -rpcallowip=0.0.0.0/0
      - -rpcuser=bchn
      - -rpcpassword=bchn_local_rpc_pw_2f9c
      - -rpcworkqueue=512
      - -dbcache=512
      - -maxmempool=300
      - -txindex=1
      - -maxconnections=64
      - -zmqpubhashblock=tcp://0.0.0.0:28332   # for SoloStrike Cash (lowest-latency)
    ports:
      - 8335:8335   # BCH P2P — forward this on your router for clearnet inbound peers

  web:
    image: ghcr.io/danhaus93-ops/bchn-dashboard:latest
    restart: on-failure
    user: "0:0"
    volumes:
      - ${APP_DATA_DIR}/data/bitcoind:/nodedata
    depends_on:
      - bitcoind
    environment:
      PORT: 3000
      TOR_IP: ${APP_GAVIN_BITCOIN_CASH_NODE_TOR_IP}
      RPC_HOST: bitcoind
      RPC_PORT: "8332"
      RPC_USER: bchn
      RPC_PASS: bchn_local_rpc_pw_2f9c
      PUBLIC_HOST: ${DEVICE_DOMAIN_NAME}
