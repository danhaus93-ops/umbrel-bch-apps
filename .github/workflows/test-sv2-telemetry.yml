name: SV2 telemetry regression suite
on:
  workflow_dispatch:
  push:
    branches: [ master, main ]
    paths: [ 'sslabs-solostrike-cash/dashboard/**', 'tests/sv2-telemetry/**', 'sv2/bridge/**', 'sv2/pool/**', 'pools-v2.json', '*/umbrel-app.yml', 'dashboard/**', 'sslabs-bitcoin-cash-node/**' ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: pip install pyyaml
      - run: bash tests/sv2-telemetry/run.sh
