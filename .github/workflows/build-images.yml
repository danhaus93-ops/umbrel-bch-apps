name: Build app images

on:
  workflow_dispatch:
  push:
    branches: [ master, main ]
    paths:
      - 'gavin-solostrike-cash/dashboard/**'
      - 'gavin-solostrike-cash/pool/**'
      - 'gavin-fulcrum/web/**'
      - 'dashboard/**'
      - '.github/workflows/build-images.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - context: ./dashboard
            image: bchn-dashboard
          - context: ./gavin-solostrike-cash/dashboard
            image: solostrike-cash-dashboard
          - context: ./gavin-solostrike-cash/pool
            image: asicseer-pool
          - context: ./gavin-fulcrum/web
            image: fulcrum-info
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          platforms: linux/amd64
          push: true
          tags: ghcr.io/danhaus93-ops/${{ matrix.image }}:latest
